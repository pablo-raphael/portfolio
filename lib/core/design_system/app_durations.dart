class AppDurations {
  static const Duration fast = Duration(milliseconds: 180);
  static const Duration medium = Duration(milliseconds: 380);
  static const Duration scroll = Duration(milliseconds: 500);
  static const Duration slow = Duration(milliseconds: 1000);
  static const Duration snackbar = Duration(seconds: 8);
}
